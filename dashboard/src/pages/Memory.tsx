// Memory page (stub) — will list/search learned user memories (Phase 3 store).
export default function Memory() {
  return (
    <div className="p-6">
      <h1 className="mb-2 text-xl font-semibold">Memory</h1>
      <p className="text-sm text-gray-500">
        Learned facts about you (Mem0 + agentmemory). Browsing & editing land here.
      </p>
    </div>
  );
}
