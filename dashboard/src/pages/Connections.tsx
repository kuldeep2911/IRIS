// Connections page (stub) — MCP server health + OAuth connections.
export default function Connections() {
  return (
    <div className="p-6">
      <h1 className="mb-2 text-xl font-semibold">Connections</h1>
      <p className="text-sm text-gray-500">
        MCP servers (browser, memory, gmail, calendar, desktop) and their status.
      </p>
    </div>
  );
}
